import { AppAlphabetOnlyDirective } from './app-alphabet-only.directive';

describe('AppAlphabetOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new AppAlphabetOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
