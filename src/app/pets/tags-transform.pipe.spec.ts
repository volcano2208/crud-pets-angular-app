import { TagsTransformPipe } from './tags-transform.pipe';

describe('TagsTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new TagsTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
