import { DisplayTagPipe } from './display-tag.pipe';

describe('DisplayTagPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayTagPipe();
    expect(pipe).toBeTruthy();
  });
});
